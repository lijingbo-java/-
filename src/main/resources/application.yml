server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mp_student?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
mybatis-plus:
  type-aliases-package: com.example.demo.entity
  mapper-locations: classpath:com/example/demo/mapper/xml/*.xml